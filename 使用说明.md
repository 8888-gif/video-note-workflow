# B 站批量下载使用说明

> 仅用于下载你有权保存的内容，例如自己的视频、公开授权材料或个人学习留存内容。请不要用于绕过付费、会员、版权或平台访问限制。

## 文件说明

| 文件 | 用途 |
|---|---|
| `urls.txt` | 待下载链接列表，一行一个链接 |
| `download_bilibili.ps1` | 批量下载脚本 |
| `downloads/` | 下载后自动生成的视频目录 |
| `cookies.txt` | 可选，用于需要登录状态的视频 |

## 基本用法

1. 打开 `urls.txt`。
2. 把 B 站视频链接按顺序粘贴进去，一行一个。
3. 在 PowerShell 中进入本文件夹。
4. 运行：

```powershell
.\download_bilibili.ps1
```

## 普通清晰度下载

默认命令会优先下载不需要合并的 `mp4` 文件：

```powershell
.\download_bilibili.ps1
```

这种方式对环境要求低，但清晰度可能不是最高。

## 高清下载

如果需要尽量下载更高清的视频和音频，可以运行：

```powershell
.\download_bilibili.ps1 -HighQuality
```

注意：高清模式常常需要 `ffmpeg` 合并音视频。如果没有安装 `ffmpeg`，可能会下载失败或无法合并。

当前工具已经安装并配置了 FFmpeg。后续通过图形界面下载时，会自动把 B 站分离的视频流和音频流合并为一个 `.mp4` 文件。

如果之前已经下载出了分离文件，例如 `.f100026.mp4` 和 `.f30280.m4a`，可以在图形界面点击 `合并已下载音视频`，工具会自动合并为正常 `.mp4`，并删除分离的源文件。

## 使用登录 Cookies

如果视频需要登录状态、收藏夹、稍后再看或更高清权限，可以导出浏览器 cookies 为 `cookies.txt`，放到本文件夹，然后运行：

```powershell
.\download_bilibili.ps1 -UseCookies
```

也可以同时使用高清模式：

```powershell
.\download_bilibili.ps1 -UseCookies -HighQuality
```

## 输出位置

下载的视频会保存到：

```text
downloads/UP主名称/视频标题 [视频ID].扩展名
```

脚本会自动生成 `downloads/downloaded.txt`，记录已经下载过的视频。再次运行时会跳过已下载内容，适合断点续传和分批补充链接。

## 上传到百度网盘

桌面快捷入口打开的是图形界面，里面新增了 `上传到百度网盘` 区域。

### 方式一：选择文件后打开百度网盘上传

1. 点击 `批量选择文件`。
2. 选择已经下载好的视频文件，可以按住 `Ctrl` 或 `Shift` 多选。
3. 点击 `打开百度网盘`。
4. 工具会打开百度网盘客户端，并打开所选文件所在目录。
5. 回到百度网盘客户端，点击 `上传`，选择刚才的文件即可。

说明：工具会把所选文件路径复制到剪贴板，方便你核对文件位置。

如果要上传下载目录中的全部完整视频，可以直接点击 `全选下载视频`，工具会自动选择下载目录下所有 `.mp4`、`.mkv`、`.flv`、`.webm`、`.mov`、`.avi` 文件，并跳过分离的临时音视频流文件。

### 方式二：复制到百度网盘同步目录

如果你已经在百度网盘客户端中配置了本地同步文件夹，可以使用这种方式。

1. 点击 `批量选择文件` 选择要上传的视频，或点击 `全选下载视频` 自动选中下载目录中的全部视频。
2. 在 `同步目录` 中选择你的百度网盘本地同步目录。
3. 点击 `批量上传到同步目录`。
4. 文件会被复制到该同步目录，百度网盘客户端会自动同步上传。

注意：如果 `Sync folder` 不是百度网盘客户端配置过的同步目录，文件只会复制到本地，不会自动上传。你可以在百度网盘客户端左侧 `同步` 功能中设置同步目录。

## 常见问题

| 问题 | 处理方法 |
|---|---|
| 提示找不到 `yt-dlp` | 脚本会自动通过 `pip` 安装 |
| 视频清晰度不高 | 尝试 `-HighQuality`，必要时安装 `ffmpeg` |
| 需要登录才能下载 | 导出 `cookies.txt` 后使用 `-UseCookies` |
| 下载中断 | 重新运行脚本，会自动续传 |
| 文件名太长或有特殊字符 | `yt-dlp` 通常会自动处理，少数情况下可改短输出模板 |
| 复制到同步目录后没有上传 | 请确认该目录已经在百度网盘客户端中设置为同步目录 |
